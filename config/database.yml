development:
  sessions:
    default:
      database: unagi_development
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: unagi_production
      hosts:
        - <%= ENV['MONGOLAB_URI'] %>
test:
  sessions:
    default:
      database: unagi_test
      hosts:
        - localhost:27017
