development:
  sessions:
    default:
      database: unagi_development
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: unagi_production
      hosts:
        - localhost:27017
test:
  sessions:
    default:
      database: unagi_test
      hosts:
        - localhost:27017
